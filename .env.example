# API URL for production deployment (Railway)
# Replace this with the actual URL of your deployed backend service.
# For local development, this can remain commented out as it defaults to localhost.

# VITE_API_URL=https://your-backend-service-url.up.railway.app/api
